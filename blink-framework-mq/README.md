# blink=framework-mq

目前处于demo状态，待开发 

目标：封装rabbitma依赖 对外提供易用的api 支持外部配置>默认配置
 
1.对mq的收 发方式进行封装 对外提供简便的调用方式
       其中包括 mq默认统一配置 也提供额外配置使外部配置文件覆盖默认项
       对外提供注解的方式进行收发
2 统一消息的格式为json格式
      提供消息实体类

3 提供不同业务类型消息模式 强业务类型 可靠性投递 消费确认  简单类型


最终构想可能会使用spring cloud stream进行更高层的抽象 但是需要新建模块了
未来提供kafaka、rocketmq等模块