# 开发环境配置

# API请求模式: gateway(网关模式) | direct(直连模式)
VITE_API_MODE=gateway

# 网关地址 (gateway-reactive 服务)
VITE_GATEWAY_URL=http://localhost:8002

# 直连base-app地址
VITE_DIRECT_URL=http://localhost:8001
