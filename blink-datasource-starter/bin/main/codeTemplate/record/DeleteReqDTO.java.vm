
package ${reqPackage};

import java.io.Serializable;
import jakarta.validation.constraints.NotNull;
import java.util.List;
/**
 * <p>
 * ${deleteReqDTOName}删除$!{table.comment}请求参数对象
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
public record ${deleteReqDTOName}(
    ## ----------  BEGIN 字段循环遍历  ----------
    #foreach($field in ${table.fields})


        #if(${field.keyFlag})
        ## 主键
        /**
         * $!{field.comment}
         */
         ${field.propertyType} deleteId,

        /**
         * 批量删除用户Id集合
         */
         List<${field.propertyType}> idList,

        #end
    #end
    /**
    * 是否批量删除标志
    */
    @NotNull
    Boolean isBatchDelete

) implements Serializable {


}
